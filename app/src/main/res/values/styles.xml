<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SpinnerStyleDropDown" parent="@style/Widget.AppCompat.Spinner.DropDown">
        <item name="android:minHeight">40dp</item>
        <item name="android:paddingLeft">2dp</item>
        <item name="android:dropDownVerticalOffset">0dp</item>
        <item name="android:background">@drawable/spinner_background</item>
        <item name="android:popupBackground">@drawable/spinner_drop_down_background</item>
        <item name="android:dropDownSelector">@android:color/white</item>
        <item name="android:drawSelectorOnTop">true</item>
        <item name="android:spinnerMode">dropdown</item>
        <item name="android:overlapAnchor">false</item>
    </style>

    <style name="SpinnerStyleDialog" parent="@style/Widget.AppCompat.Spinner">
        <item name="android:minHeight">34dp</item>
        <item name="android:paddingLeft">2dp</item>
        <item name="android:paddingRight">18dp</item>
        <item name="android:background">@drawable/spinner_background</item>
        <item name="android:clickable">true</item>
        <item name="android:spinnerMode">dialog</item>
    </style>

    <style name="SpinnerStyle" parent="@style/SpinnerStyleDropDown" />

</resources>
